import React, { ReactNode } from 'react';

import { AlertProvider } from './alert/alert';

type AppProviderI = {
    children: React.ReactNode
}

/* const AppProvider: React.FC<AppProviderI> = ({children}) => (
    
);
export AppProvider; */